import { flexCenter, font } from '../../global/styles';

export const styles = {
  bodyContainer: Object.assign(flexCenter("column"), {
    backgroundColor: "rgba(240, 240, 240, 1)"
  }),
  bodyText: Object.assign(font, {
    textAlign: "center"
  })
}
